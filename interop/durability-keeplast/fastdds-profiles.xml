<?xml version="1.0" encoding="UTF-8"?>
<!-- LOOPBACK-ONLY Fast DDS profile for the WP-DURABILITY-SERVICE-TRANSIENT interop DoD
     (DDS 1.4 §2.2.3.4, ADR 0021). interfaceWhiteList is pinned to 127.0.0.1 ONLY (NOT the LAN
     address) so Fast DDS routes BOTH discovery and the user dataflow over lo0. UDPv4 only.
     The TRANSIENT_LOCAL + KEEP_ALL durability is set in C++ via the DURABILITY env gate,
     NOT here. Copy to ./profiles.xml in the run cwd (the shapes binaries load_XML_profiles_file
     from cwd). Capture-only aid; NOT built or run by CI. -->
<dds xmlns="http://www.eprosima.com/XMLSchemas/fastDDS_Profiles">
  <profiles>
    <transport_descriptors>
      <transport_descriptor>
        <transport_id>udp_loopback</transport_id>
        <type>UDPv4</type>
        <interfaceWhiteList>
          <address>127.0.0.1</address>
        </interfaceWhiteList>
      </transport_descriptor>
    </transport_descriptors>
    <participant profile_name="fastdds_interop" is_default_profile="true">
      <rtps>
        <useBuiltinTransports>false</useBuiltinTransports>
        <userTransports>
          <transport_id>udp_loopback</transport_id>
        </userTransports>
        <propertiesPolicy>
          <properties>
            <property>
              <name>fastdds.type_propagation</name>
              <value>enabled</value>
            </property>
          </properties>
        </propertiesPolicy>
      </rtps>
    </participant>
  </profiles>
</dds>
