<?xml version="1.0"?>
<!-- LOOPBACK-ONLY + TRANSIENT_LOCAL profile for the WP-DURABILITY-SERVICE-TRANSIENT interop DoD
     (DDS 1.4 §2.2.3.4, ADR 0021). Foreign Connext 7.3.1 publisher and late-joining subscriber.
     Two jobs:

     1. Loopback pinning (allow_interfaces=127.0.0.1, UDPv4 only): our service node binds
        to 127.0.0.1, sends unicast SPDP to 127.0.0.1:7410 (the well-known SPDP unicast port
        for domain 0 participant 0), and every RTPS byte rides loopback. Mirrors
        interop/durability-transient-local/USER_QOS_PROFILES.xml.

     2. TRANSIENT_LOCAL durability + KEEP_ALL history (the feature). The committed shapes_pub.cxx /
        shapes_sub.cxx read default_datawriter_qos() / default_datareader_qos() and override
        ONLY reliability in code; durability + history come from this is_default_qos profile.
        resource_limits unlimited so all pre-exit samples are retained and delivered.

     Capture-only aid; NOT built or run by CI.
     Run from THIS directory so the binary loads USER_QOS_PROFILES.xml from cwd. -->
<dds xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
     xsi:noNamespaceSchemaLocation="https://community.rti.com/schema/7.3.1/rti_dds_qos_profiles.xsd">
  <qos_library name="InteropDurabilityTL">
    <qos_profile name="LoopbackTransientLocal" is_default_qos="true">
      <participant_qos>
        <transport_builtin>
          <mask>UDPv4</mask>
        </transport_builtin>
        <property>
          <value>
            <element>
              <name>dds.transport.UDPv4.builtin.parent.allow_interfaces</name>
              <value>127.0.0.1</value>
            </element>
          </value>
        </property>
      </participant_qos>
      <datawriter_qos>
        <reliability>
          <kind>RELIABLE_RELIABILITY_QOS</kind>
        </reliability>
        <durability>
          <kind>TRANSIENT_LOCAL_DURABILITY_QOS</kind>
        </durability>
        <history>
          <kind>KEEP_ALL_HISTORY_QOS</kind>
        </history>
        <resource_limits>
          <max_samples>LENGTH_UNLIMITED</max_samples>
          <max_instances>LENGTH_UNLIMITED</max_instances>
          <max_samples_per_instance>LENGTH_UNLIMITED</max_samples_per_instance>
        </resource_limits>
      </datawriter_qos>
      <datareader_qos>
        <reliability>
          <kind>RELIABLE_RELIABILITY_QOS</kind>
        </reliability>
        <durability>
          <kind>TRANSIENT_LOCAL_DURABILITY_QOS</kind>
        </durability>
        <history>
          <kind>KEEP_ALL_HISTORY_QOS</kind>
        </history>
        <resource_limits>
          <max_samples>LENGTH_UNLIMITED</max_samples>
          <max_instances>LENGTH_UNLIMITED</max_instances>
          <max_samples_per_instance>LENGTH_UNLIMITED</max_samples_per_instance>
        </resource_limits>
      </datareader_qos>
    </qos_profile>
  </qos_library>
</dds>
