<?xml version="1.0"?>
<!-- LOOPBACK-ONLY + TRANSIENT_LOCAL profile for the WP-DURABILITY-TRANSIENT-LOCAL late-joiner interop
     DoD (DDS 1.4 §2.2.3.4) with this stack's Shapes harness. Two jobs:

     1. Loopback pinning (allow_interfaces=127.0.0.1, UDPv4 only): the committed interop/connext/shapes-*
        binaries bind+advertise ONLY lo0 so every RTPS byte rides loopback (sidesteps the macOS app-firewall
        LAN-UDP drop + the stale-pinned-LAN participant-index init failure). The Lisp side reaches this peer
        with unicast SPDP to 127.0.0.1:7410 (PEERS=127.0.0.1:7410). UDPv4-only keeps the wire observable.
        Mirrors interop/keyed-flatdata/connext/USER_QOS_PROFILES.xml.

     2. TRANSIENT_LOCAL durability (the feature). The committed shapes_pub.cxx / shapes_sub.cxx read
        default_datawriter_qos() / default_datareader_qos() and override ONLY reliability in code; durability
        + history are taken from this is_default_qos profile. The writer RETAINS its published history and
        REPLAYS it to a late-joining reader; a TRANSIENT_LOCAL reader REQUESTS a matched writer's pre-join
        history. reliability=RELIABLE (TL is reliable-based) + history KEEP_ALL so ALL pre-join samples are
        retained (depth-1 KEEP_LAST would replay only the latest). resource_limits unlimited.

     Capture-only aid; NOT built or run by CI. -->
<dds xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
     xsi:noNamespaceSchemaLocation="https://community.rti.com/schema/7.3.1/rti_dds_qos_profiles.xsd">
  <qos_library name="InteropDurabilityTL">
    <qos_profile name="LoopbackTransientLocal" is_default_qos="true">
      <participant_qos>
        <transport_builtin>
          <mask>UDPv4</mask>
        </transport_builtin>
        <property>
          <value>
            <element>
              <name>dds.transport.UDPv4.builtin.parent.allow_interfaces</name>
              <value>127.0.0.1</value>
            </element>
          </value>
        </property>
      </participant_qos>
      <datawriter_qos>
        <reliability>
          <kind>RELIABLE_RELIABILITY_QOS</kind>
        </reliability>
        <durability>
          <kind>TRANSIENT_LOCAL_DURABILITY_QOS</kind>
        </durability>
        <history>
          <kind>KEEP_ALL_HISTORY_QOS</kind>
        </history>
        <resource_limits>
          <max_samples>LENGTH_UNLIMITED</max_samples>
          <max_instances>LENGTH_UNLIMITED</max_instances>
          <max_samples_per_instance>LENGTH_UNLIMITED</max_samples_per_instance>
        </resource_limits>
      </datawriter_qos>
      <datareader_qos>
        <reliability>
          <kind>RELIABLE_RELIABILITY_QOS</kind>
        </reliability>
        <durability>
          <kind>TRANSIENT_LOCAL_DURABILITY_QOS</kind>
        </durability>
        <history>
          <kind>KEEP_ALL_HISTORY_QOS</kind>
        </history>
        <resource_limits>
          <max_samples>LENGTH_UNLIMITED</max_samples>
          <max_instances>LENGTH_UNLIMITED</max_instances>
          <max_samples_per_instance>LENGTH_UNLIMITED</max_samples_per_instance>
        </resource_limits>
      </datareader_qos>
    </qos_profile>
  </qos_library>
</dds>
