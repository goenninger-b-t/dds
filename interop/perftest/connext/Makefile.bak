# Connext side of the cross-stack parity harness (WP-CONFORMANCE-AND-PARITY WP-1).
# Reuses the shared Connext build rules; the IDL is the parity type, not ShapeType.
APPS   := perf_responder perf_pinger
COMMON := ../../connext/common
IDL    := ../common/PerfData.idl
include $(COMMON)/common.mk
