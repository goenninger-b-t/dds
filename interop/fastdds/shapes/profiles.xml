<?xml version="1.0" encoding="UTF-8"?>
<!-- FR-IO-2 harness profile: UDPv4 only (observable on lo0/en*), TypeLookup
     client+server explicitly on. interfaceWhiteList: EDIT per machine.

     Fast DDS 3.x note: the 2.x <builtin><typelookup_config> element no longer
     exists (not in fastdds_profiles.xsd). TypeLookup request+reply endpoints
     (client AND server) are created whenever the participant property
     fastdds.type_propagation is "enabled" (the default) - pinned explicitly
     below so the harness intent survives any future default change. -->
<dds xmlns="http://www.eprosima.com/XMLSchemas/fastDDS_Profiles">
  <profiles>
    <transport_descriptors>
      <transport_descriptor>
        <transport_id>udp_only</transport_id>
        <type>UDPv4</type>
        <interfaceWhiteList>
          <address>127.0.0.1</address>
          <address>192.168.2.148</address>
        </interfaceWhiteList>
      </transport_descriptor>
    </transport_descriptors>
    <participant profile_name="fastdds_interop" is_default_profile="true">
      <rtps>
        <useBuiltinTransports>false</useBuiltinTransports>
        <userTransports>
          <transport_id>udp_only</transport_id>
        </userTransports>
        <propertiesPolicy>
          <properties>
            <property>
              <name>fastdds.type_propagation</name>
              <value>enabled</value>
            </property>
          </properties>
        </propertiesPolicy>
      </rtps>
    </participant>
  </profiles>
</dds>
