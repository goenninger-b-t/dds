# NFR-MEM allocation ratchet (ADR 0062). One ceiling PER ARCHITECTURE, in bytes/sample.
#
# ⚠️ RE-BASELINED 2026-07-21 (workload) AND RAISED 2026-07-21 (ADR 0078 REVERTED). Neither number below is
# comparable to a row from before that date. Two separate things moved them:
#
# 1. THE WORKLOAD. mem-per-sample went from 3000 to 60000 samples because the gate had become BLUNTER THAN
#    THE WINS IT GUARDS: the measured window carries a FIXED ~65 KB per-run allocation occurring 0-3 times,
#    which at 3000 samples landed as a ~22 B/sample quantum — ONE UNCHANGED ARM read 1791 / 1813 / 1835 /
#    1857, a ~65 B spread against a typical slice's ~35 B win. The quantum is per RUN, so it shrinks as
#    1/samples: spread ~65 B at 3000, ~3 B at 30000, ~0.4 B back-to-back at 60000 (~3 B across a session).
#    Changing `samples` is therefore a re-baseline of BOTH rows, never a free knob.
#
# 2. ADR 0078 WAS REVERTED, so its ~-33 B/sample went with it and the ceilings went back UP. The RX
#    store-copy pool caused HEAP CORRUPTION on Linux — proven by isolation on a real Linux box, see the ADR.
#    This is the documented exception to "the ratchet only moves DOWN": a reverted win is not a regression,
#    and pretending otherwise would leave a ceiling no code can meet.
#
# BOTH ROWS ARE MEASURED, at 60000 samples, and x86_64 is for the first time measured DIRECTLY rather than
# predicted from a CI reading or derived from the other arch (a prediction that had been 58 B off):
#   arm64   1878.4-1880.6   (dev box, Apple silicon, SBCL)
#   x86_64  1916.8-1918.9   (Ubuntu 24.04 x86_64, SBCL 2.2.9 — the same OS family and arch as CI)
# Ceilings are set ~30 B above the measured maximum, about 10x the session-scale spread.
#
# The ratchet only moves DOWN, except for the two documented re-baselines above (a workload change or a
# reverted win — and either must say so here). Lower a row when you bank a win on THAT arch.
#
# ⚠️ YOU CAN ONLY LOWER THE ROW FOR THE ARCH YOU MEASURED ON. gate-mem reads the row for `uname -m`, so a
# win banked on the dev box lowers arm64 and leaves x86_64 stale — and the ratchet then FAILS ON
# IMPROVEMENT in CI, which is red until someone reads the CI number and lowers that row too. This is not
# hypothetical: CI was red from the 5th TX slice for exactly this reason. AFTER a win, check the CI
# gate-mem number and lower the OTHER arch's row in the same or an immediate follow-up commit.
#
# LOWERED 2026-07-26 (ADR 0087 — the TX key-hash serialization scratch is CAS-borrowed). BOTH rows were
# measured directly on this slice, and the two arches moved by materially different amounts, which is why
# neither was derived from the other:
#   arm64   1852.2 -> 1769.8   (-82.4)   dev box, Apple silicon, SBCL
#   x86_64  1902.8 -> 1777.4   (-125.4)  Ubuntu 24.04 x86_64, SBCL — same-image A/B (the shipped body
#                                        measured, then %write-key-hash redefined to its pre-0087 body)
# The x86_64 row had also gone stale since 2026-07-21 (wins landed on both arches without it being
# re-measured), so its drop is larger than this slice alone; the A/B above isolates the slice's own share.
#
# LOWERED 2026-07-26 (ADR 0088 — the control-path proxy-key cache). Both rows measured directly again,
# and again the two arches moved by materially different amounts, which is why neither is ever derived
# from the other:
#   arm64   1769.8 -> 1742.1  (-27.7)
#   x86_64  1777.4 -> 1704.5  (-72.9)
# ⚠️ TWO CEILINGS PER ARCH SINCE 2026-07-28 (ADR 0093 slice 1). Format:
#
#     <arch>  <ceiling-COPY>  <ceiling-RETURN>
#
#   COPY   — the legacy workload: the application takes samples and DROPS them. Unchanged, and every
#            historical row above refers to this column, so it stays comparable across the whole campaign.
#   RETURN — the application RETURN-LOANs each taken sample, i.e. honours the ADR 0093 loan contract, so
#            the reader can recycle its delivery wrappers. This is the workload a zero-allocation
#            application actually runs, and the only one in which the recycling can be seen at all.
#
# `-` means NOT YET MEASURED ON THIS ARCH. gate-mem still MEASURES that arm and PRINTS the number (with
# the row to paste in), but does not ratchet it — so an unmeasured arch reports the figure it needs
# instead of going red. ⚠️ NEVER fill a `-` from the other arch's number: the two diverge materially and
# unpredictably (ADR 0087 −82.4 vs −125.4; ADR 0088 −27.7 vs −72.9), and a predicted x86_64 value has
# already been 58 B wrong once. Measure it on that arch, or leave the dash.
#
# RETURN ceilings measured 2026-07-28, each run in its own process on its own domain:
#   arm64  slice 1 (wrapper pooling): 1569.5 / 1567.2 / 1567.6 -> ceiling was 1600
#   arm64  slice 4 (data-struct pooling, ADR 0093 §11): 1534.9 / 1534.0 / 1536.3 -> ceiling was 1570
#
# LOWERED 2026-07-28 (%ptr+ inlined — BOTH columns, because every pshared operation in the stack paid a
# boxed system-area-pointer). One `declaim inline` took a PSHARED-LOCK+UNLOCK pair from 31.85 B/iteration
# to 0.00, and the end-to-end path by -112 B/sample on BOTH arms:
#   arm64 COPY   1740.7 -> 1628.3 / 1630.5 / 1628.3   -> LOWERED 1775 -> 1660
#   arm64 RETURN 1534.9 -> 1422.5 / 1423.7 / 1426.3   -> LOWERED 1570 -> 1460
# LOWERED 2026-07-28 (static-pointer inlined — the SAME defect as %ptr+, one level down: an out-of-line
# defun* returning a system-area-pointer boxes it, on every raw sendto/recvfrom and every SHMEM sap-copy):
#   arm64 COPY   1628.3 -> 1563.8 / 1566.8 / 1566.0   -> LOWERED 1660 -> 1600
#   arm64 RETURN 1422.5 -> 1358.8 / 1360.4 / 1358.5   -> LOWERED 1460 -> 1390
#
# LOWERED 2026-07-28 (both clocks: FLET -> MACROLET, so the scratch pointer never crosses an out-of-line
# call and is never boxed — zero-allocation on ANY thread, not just PAL-spawned ones):
#   arm64 COPY   1550.0 / 1553.1 / 1550.2   -> LOWERED 1600 -> 1585
#   arm64 RETURN 1343.3 / 1343.3 / 1342.2   -> LOWERED 1390 -> 1375
#
# LOWERED 2026-07-28 (the datagram CURSOR is reused per receiver thread instead of consed per datagram —
# a 6-word struct, 2.05 datagrams/sample). Predicted 98.4 B/sample from the receive-pipeline bisect
# (bench/report/2026-07-28-the-receive-path-consed-a-cursor-per-datagram.md); measured -97.4 / -97.6:
#   arm64 COPY   1452.6 / 1456.6 / 1452.6   -> LOWERED 1585 -> 1490
#   arm64 RETURN 1244.6 / 1245.7 / 1245.2   -> LOWERED 1375 -> 1280
#
# LOWERED 2026-07-28 (the source GUID-prefix is CACHED per receiver thread instead of copied out of the
# datagram every time — a 12-octet vector, 2.05 datagrams/sample). It is a write-once CACHE, not a reused
# scratch: the prefix is copied out precisely so callers may RETAIN it. Predicted 65.7 B/sample, measured
# -63.3 / -62.8 (bench/report/2026-07-28-the-receive-path-copied-the-source-prefix-per-datagram.md):
#   arm64 COPY   1389.3 / 1389.3 / 1389.1   -> LOWERED 1490 -> 1420
#   arm64 RETURN 1183.8 / 1181.8 / 1181.6   -> LOWERED 1280 -> 1215
#
# LOWERED 2026-07-28 (%matched-reader-keys is MEMOIZED on the node instead of rebuilt on every inbound
# ACKNACK — a node-locked list of every matched endpoint, then a copy-seq per reader GUID and a cons per
# key, for a value that changes only on match/unmatch/prune. It shares the sibling destination memos'
# invalidation seam, %invalidate-dest-cache. Four hot-path callers, so the win is larger than the ~50 B
# the single probed call showed:
#   arm64 COPY   1329.2 / 1329.4 / 1328.1   -> LOWERED 1420 -> 1360
#   arm64 RETURN 1120.3 / 1117.5 / 1121.7   -> LOWERED 1215 -> 1155
#
# LOWERED 2026-07-28 (the source GUID is CACHED per receiver thread — the RX complement of ADR 0088. A
# fresh 16-octet GUID was built per sample in %on-user-data, ANOTHER for the same (prefix, writer) in
# %deliver-user-sample, and a third per HEARTBEAT in %on-user-heartbeat. Write-once cache, never a reused
# scratch: the GUID is RETAINED as the proxy key, the store's outer key, the dedup key, the ZC loan handle
# and SampleInfo.publication_handle. Predicted 96 B/sample (3 x 32), measured -97.2 / -102.5:
#   arm64 COPY   1232.0 / 1228.5 / 1232.5   -> LOWERED 1360 -> 1265
#   arm64 RETURN 1017.8 / 1023.8 / 1021.6   -> LOWERED 1155 -> 1055
#
# LOWERED 2026-07-28 (the TX sample serializer is an flet declared DYNAMIC-EXTENT instead of a RETURNED
# closure — a closure that is returned must be heap-allocated, ~48 B on every write; ADR 0072's downward-
# funarg idiom, which publish-sample-into satisfies exactly). Predicted ~48, measured -49.3 / -41.7:
#   arm64 COPY   1182.7 / 1181.9 / 1182.2   -> LOWERED 1265 -> 1215
#   arm64 RETURN  976.1 /  975.9 /  975.9   -> LOWERED 1055 -> 1010
#
# LOWERED 2026-07-28 (the PID_KEY_HASH inline-QoS block is CACHED per instance instead of rebuilt on every
# KEYED write out of three fresh objects — a 24-octet vector, an octet-buffer wrapper and a cursor, 128
# B/call. It is a pure function of the key hash, i.e. of the INSTANCE, and a writer has few instances and
# writes many samples. The block validates itself: its octets [4,20) ARE the hash. Measured -125.4/-128.1:
#   arm64 COPY   1057.3 / 1056.3 / 1055.5   -> LOWERED 1215 -> 1090
#   arm64 RETURN  848.0 /  846.8 /  848.2   -> LOWERED 1010 ->  880
#
# LOWERED 2026-07-28 (writer-write's change-builder is an flet declared DYNAMIC-EXTENT instead of a heap
# lambda capturing ELEVEN variables (~104 B/write), AND the CHANGE it smuggled out through a closed-over
# MUTABLE variable — a heap value cell of its own on SBCL — is now RETURNED by %writer-add-bounded, which
# had it in hand all along. Predicted ~120 B/sample, measured -129.5 / -123.9:
#   arm64 COPY    927.8 /  926.7 /  928.4   -> LOWERED 1090 -> 960
#   arm64 RETURN  724.1 /  721.9 /  721.9   -> LOWERED  880 -> 755
#
# LOWERED 2026-07-28 (%send-msg-buf takes its build cursor from the receiver thread's own REPLY-cursor slot
# instead of consing one per send — a receiver answers every inbound HEARTBEAT with an ACKNACK from inside
# dispatch, so this ran ~once per sample at 48 B. It needs a slot DISTINCT from the inbound cursor, which is
# live at that moment. Predicted ~49 B/sample, measured -47.3 / -50.3:
#   arm64 COPY    880.5 /  879.3 /  882.3   -> LOWERED 960 -> 915
#   arm64 RETURN  673.8 /  676.0 /  674.9   -> LOWERED 755 -> 706
#
# LOWERED 2026-07-28 (the ACKNACK RECEIVE path stops consing, twice: the resolved-destination arm repairs to
# its ONE peer DIRECTLY instead of wrapping it as (list (cons ...)) for a DOLIST that took it apart again,
# and the inbound SequenceNumberSet is parsed into the receiver thread's own scratch. The bitmap scratch is
# threaded from the ONE call site whose consumer was proven not to retain it (writer-on-acknack only walks
# the bits under the writer lock) and is never defaulted on inside the parser. Measured -59.9 / -66.6:
#   arm64 COPY    820.6 /  818.9 /  819.1   -> LOWERED 915 -> 850
#   arm64 RETURN  607.2 /  609.4 /  608.3   -> LOWERED 706 -> 640
#
# LOWERED 2026-07-28 (hc-purge-below collects into the HistoryCache's REUSED purge scratch instead of a
# fresh list built by pushing onto a closed-over MUTABLE variable — a value cell plus a cons per purged SN,
# on every inbound ACKNACK. ⚠️ PREDICTED ~64 B/sample AND OVER-SHOT: measured only -18.0 / -13.1, the first
# over-prediction of the campaign — SBCL was evidently already stack-allocating the MAPHASH closure, so only
# the value cell and the cons were ever real. Predict from the source, but SIZE WITH THE GATE, in BOTH
# directions:
#   arm64 COPY    802.6 /  801.5 /  803.7 /  796.0   -> LOWERED 850 -> 835
#   arm64 RETURN  594.1 /  594.1 /  594.1 /  594.1   -> LOWERED 640 -> 625
#
# LOWERED 2026-07-29 (the DATA_AVAILABLE listener lookup WALKS the containment chain instead of consing it.
# %listener-ancestry returned reader->Subscriber->participant as a fresh LIST, and the notification runs it
# TWICE per sample on the receiver thread — 5 conses, 80 B — to build two lists that were immediately
# walked and dropped. It measured as the ENTIRE DATA-handler cost (89.5 of 91.7 B/sample). Predicted ~80,
# measured -76.4 / -80.8:
#   arm64 COPY    719.6 /  720.7 /  720.7   -> LOWERED 835 -> 750
#   arm64 RETURN  513.3 /  513.3 /  514.4   -> LOWERED 625 -> 545
#
# LOWERED 2026-07-29 (the drain skips its merged PLAN when no lifecycle change is pending — the plan exists
# only to tag each key data-vs-lifecycle and cost a CONS PER KEY to do it, while a dispose/unregister is
# rare. With none pending, DATA-KEYS is already the whole plan: sorted in place, drained directly.
# Predicted ~16 B/sample (one cons per pending key), measured -14.2 / -16.4 — the probe window had said
# 49.1 for this phase, another over-report:
#   arm64 COPY    705.4 /  705.4 /  705.4   -> LOWERED 750 -> 730
#   arm64 RETURN  496.9 /  496.9 /  495.8   -> LOWERED 545 -> 525
#
# LOWERED 2026-07-29 (reader-on-heartbeat compacts its received-marker table IN ONE PASS: CLHS 18.2 permits
# REMHASH of the key currently being processed during MAPHASH, which is exactly what this does, so the
# collect-then-remove two-phase walk bought nothing. ⚠️ NOT the same as hc-purge-below, which keeps its two
# phases deliberately — that one removes via %hc-remove-change, which maintains the index and MAPHASHes the
# same table. Predicted ~32 (value cell + cons), measured -17.5 / -15.3 — as in the purge slice, the value
# cell rode the already-stack-allocated closure and only the cons was real:
#   arm64 COPY    687.9 /  687.9 /  689.0   -> LOWERED 730 -> 720
#   arm64 RETURN  481.6 /  480.6 /  480.5   -> LOWERED 525 -> 510
#
# LOWERED 2026-07-29 (the TX single-datagram fast path reuses the node's build cursor instead of consing one
# per datagram — the FOURTH site of the same 48 B struct this campaign. The cursor is paired with TX-MSG and
# is no more shared than the buffer it reads; %cursor-reuse's EQ test degrades a different buffer (the async
# sender's own) to the pre-existing allocation. Predicted 48, measured -46.9 / -49.2:
#   arm64 COPY    641.0 /  639.9 /  641.0   -> LOWERED 720 -> 670
#   arm64 RETURN  432.4 /  431.4 /  431.4   -> LOWERED 510 -> 460
#
# LOWERED 2026-07-29 (the TX PAYLOAD SERIALIZER reuses the DataWriter's cursor — the FOURTH and last site of
# the same 48 B struct. It had been deferred on the claim that an EQ-keyed cache "would miss" because the
# payload pool hands out a different buffer per write; THAT CLAIM WAS WRONG — pool-acquire is a LIFO stack,
# so acquire->release->acquire returns the SAME buffer and one slot hits in steady state. Concurrency is
# safe by construction rather than by lock: the pool pops a DISTINCT buffer per caller, so two threads can
# never be handed the same cursor. Predicted 48, measured -48.1 / -47.9:
#   arm64 COPY    592.9 /  590.8 /  591.8   -> LOWERED 670 -> 625
#   arm64 RETURN  383.3 /  385.5 /  385.5   -> LOWERED 460 -> 415
#
# LOWERED 2026-07-29 (%send-changes-packed returns immediately when there is NOTHING to send. A datagram
# needs at least one submessage, so with no CHANGES and no HEARTBEAT the plan is empty and the step loop
# emits nothing — after consing the state pair and walking the planner to discover that. The ACKNACK repair
# path makes exactly that call once per sample: the steady-state ACKNACK acks everything and repairs
# nothing. Predicted ~30, measured -32.7 / -33.8:
#   arm64 COPY    560.2 /  558.0 /  560.2   -> LOWERED 625 -> 590
#   arm64 RETURN  349.5 /  352.8 /  350.6   -> LOWERED 415 -> 385
#
# LOWERED 2026-08-06 (ADR 0105 Task 5 — the deferred view-state marking stops consing a TOUCHED list. All
# four access paths (%select-samples-unlocked list-mode and into-mode, take-loaned, read-loaned) collected
# the instance handles they had selected into a fresh list with PUSHNEW :TEST EQUALP, then walked it after
# the pass to mark DR-INSTANCES — one cons per selected INSTANCE per call, plus an EQUALP list scan per
# selected sample. The DEFERRAL is load-bearing and was kept exactly (DDS 1.4 §2.2.2.5.1.4: every sample of
# a not-yet-accessed instance must read NEW within the one call that first accesses it); only the SCRATCH
# went. The marking is idempotent, so each path now walks the set it had ALREADY materialised — the wrapper
# list it returns, the SampleInfo vector it just filled, or DR-CACHE itself. Predicted 16 B/sample (one
# cons; the bench writes one sample of one instance per take), measured -16.2 / -14.2:
#   arm64 COPY    543.8 /  543.8 /  542.7   -> LOWERED 590 -> 571
#   arm64 RETURN  337.5 /  337.5 /  335.3   -> LOWERED 385 -> 355
# and measured DIRECTLY on the real x86_64 box the same day (never derived from arm64, and never emulated):
#   x86_64 COPY   579.1 /  578.1 /  578.1   -> LOWERED 625 -> 609   (was 593.0 / 594.2 / 594.3: -15.4)
#   x86_64 RETURN 386.7 /  385.6 /  386.8   -> STAYS A DASH         (was 401.6 / 401.9 / 402.5: -15.4)
# The x86_64 RETURN row is NOT re-banked off these numbers. The dash is an OPEN QUESTION about CI, not a
# missing measurement (see below); three more quiet-box readings do not answer it, and re-banking would
# repeat exactly the error the dash records. The x86_64 COPY row IS lowered: that arm has been stable in CI
# (593.7 on the run whose RETURN read 3632) and the box has agreed with CI on it to 0.2 %.
#
# ⚠️ MEASURE WITH NOTHING ELSE RUNNING. A gate-mem run taken while the test suites were running in the
# background read COPY 1710.8 and FAILED — a 160 B phantom regression from participant + CPU contention,
# not from code. A benchmark is an instrument; give it the machine.
#
# x86_64 IS now measured, on the real Linux box (192.168.2.180, SBCL 2.2.9.debian — the same distro build
# CI runs), 2026-08-06. Both columns are real measurements; neither is derived from arm64, which remains
# forbidden. COPY 593.0 / 594.2 / 594.3 over three runs and CI's own 594.4; RETURN 401.6 / 401.9 / 402.5
# and CI's 404.3. The ceilings below are the HIGHEST observed value plus the gate's 5% headroom, so a
# normal run cannot flap.
#
# ⚠️ DO NOT MEASURE THIS ARCH UNDER EMULATION. The same two arms in the linux/amd64 Docker harness read
# 603.9 / 411.0 — about 10 B/sample HIGH on both. The image runs the same distro SBCL, so the version is
# not the cause; the likely mechanism is that allocation here is TIMING-DEPENDENT (a slower CPU changes how
# many heartbeats/ACKNACKs are processed per sample), which emulation perturbs. Unconfirmed as to cause,
# but decisive as to practice: emulated numbers are not this gate's oracle. Use the real box.
#
# ⛔⛔ x86_64 RETURN IS BACK TO A DASH, AND THAT IS AN OPEN QUESTION, NOT A CLEANUP. It was banked at 425
# on 2026-08-06 from box measurements (401.6 / 401.9 / 402.5) plus one CI observation of 404.3. The very
# next CI run reported **3632.0** — a 9x jump — while COPY on the SAME run read 593.7, i.e. exactly normal.
# Generic contention would inflate BOTH arms, so this is specific to the RETURN path (:return-loans t is
# the only difference between them).
#
# NOT REPRODUCED in three environments: the box 402.1, the box PINNED TO 2 CORES 400.3 (so it is not the
# runner's core count), and the linux/amd64 Docker image 411.0. Since the platform that gates this arm is
# the one platform where its behaviour cannot be characterised, gating it was premature — banking a ceiling
# from a quiet 16-core box for an arm never observed under CI load was the error, not the dash.
#
# ⚠️ DO NOT read the dash as "3632 was noise". It may be a REAL defect only CI can see — this repo has
# found six of those (uninitialised memory on the wire, a stack that could not shut down on Linux). The
# open question is why RETURN alone explodes there; a plausible starting hypothesis is that under CI's
# conditions the app falls behind the receiver, the data pool is empty at drain time so recycling never
# happens, and reliable retransmission multiplies the per-sample work. Chase it with a CI run that PRINTS
# the pool hit/miss counts, not with another local measurement.
arm64  571 355
x86_64 609 -
