# NFR-MEM allocation ratchet (ADR 0062). One ceiling PER ARCHITECTURE, in bytes/sample.
#
# SBCL's per-sample allocation differs materially by arch — measured, not assumed:
#   arm64   1813-1857      (2026-07-21: + TASK-3 the cache-change STRUCT pool — an evicted, send-ref-free,
#                            non-ZC/pooled/pinned change is recycled to the HistoryCache change-freelist and
#                            reused by the next write (ADR 0077, ~-109 B); prior RX-pooling ADR 0073/0075/0076;
#                            floor 2118 -> 1813, cumulative ~-1747 from 3560,
#                            ADR 0062/0065-0068/0072/0073/0075/0076/0077)
#   x86_64  ~1951 (est)     (2026-07-21: was 2083 read off CI at ADR 0076; the TASK-3 struct pool (ADR 0077)
#                            drops it ~109 like arm64 -> est ~1951. NOT measured here (dev box is arm64); the
#                            2090 ceiling is set ROBUSTLY-SAFE for a floor anywhere in [1910,2010] (above
#                            max-floor+strays, below min-floor/0.9) so CI neither ratchet-demands nor regresses.
#                            Tighten to CI's actual number on the next slice. The same divergence made the
#                            FlatData vtable-vs-classic guard flip, and is why a single shared ceiling cannot work.)
#
# The ratchet only moves DOWN. Lower a row when you bank a win on THAT arch (gate-mem tells you).
#
# ⚠️ YOU CAN ONLY LOWER THE ROW FOR THE ARCH YOU MEASURED ON. gate-mem reads the row for `uname -m`, so a
# win banked on the dev box lowers arm64 and leaves x86_64 stale — and the ratchet then FAILS ON
# IMPROVEMENT in CI, which is red until someone reads the CI number and lowers that row too. This is not
# hypothetical: CI was red from the 5th TX slice for exactly this reason. AFTER a win, check the CI
# gate-mem number and lower the OTHER arch's row in the same or an immediate follow-up commit.
arm64  1900
x86_64 2090
